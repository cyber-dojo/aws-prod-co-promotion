name: Main

on:
  push:


jobs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Start...
        run:
          echo Hello world
